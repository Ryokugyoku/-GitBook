---
description: マージする手順をシナリオベースで記載する
icon: whmcs
---

# GitHub Merge Scenario



## ブランチにて同じ共通部品を利用してブランチ同士で競合するのがわかる場合

チェリーピック編

### チェリーピック競合

[前提条件](github-merge-scenario.md#qian-ti-tiao-jian)

| シナリオ                 | マージ結果                                                                               |
| -------------------- | ----------------------------------------------------------------------------------- |
| ブランチAとBの共通コードの変更が同一  | 正常にマージされる                                                                           |
| ブランチAとBの共通コードの変更が異なる | <p>競合が発生する<br>片方の共通コードが古い場合は、もう片方のブランチの共通コードのコミットを取り込むんで共通コードが同一の状態にすることでマージできる</p> |

### 理想

元となる親ブランチに対して変更が発生した場合、都度変更をブランチに適用するようにする

都度フェッチを行うことで、小さい単位での競合が起こり、未然に変更を取り込んでテストを実施できる

### 結論

どのようなパターンでも以下の条件を満たせば、マージする際の難易度が低下する

複数のブランチにて共通処理を利用している場合であっても、対象の共通ソースコードのコミットを共有し同一の状態を常に保てば大きな競合を避けれる。

仮にバグが発生し共通処理Aを改修した元のブランチで修正を行わなくとも、同様の手順を踏んで共通処理のソースコードの変更を取り入れるようにすれば競合は避けれる

### チェリーピック欠点

コミットログの追尾が困難になる

あらかじめIssueを作成し、前もって競合箇所を特定する必要がある

更にコミット規約を厳格に策定し、競合ソースをコミットする際は競合が発生するIssue番号を記載して管理し、Issue保持者に通知が飛ぶようにメンションする必要がある

### 前提条件

* ブランチAとブランチBにて同じ変更を実装する必要がある
* 同じ親から派生したブランチA、ブランチBが存在する
* 共通部品（ソースコード）がブランチA又はブランチB（いずれのうち片方）にて単一コミットされている
* 共通部品がコミットされているコミットログにて、共通部品のみのソースコードがコミットされている
* 本シナリオにおいて共通処理の改修はブランチA所有者にて行うものとする

### ノーマルシナリオ

#### ブランチA所有者

1. 共通処理を改修する
2. 共通処理のみ変更を反映するコミットを行う
3. 自信のブランチに対してプルする



#### ブランチB所有者

1. ブランチA所有者からコミット＆プルしたことを確認する
2. 以下のリンクに記載された方法で、対象コミットの変更をブランチBに適用する\


{% embed url="https://docs.github.com/ja/desktop/managing-commits/cherry-picking-a-commit-in-github-desktop" %}

### 変更発生シナリオ

**※**[**ノーマルシナリオ**](github-merge-scenario.md#nmarushinario)**実施後に問題が発生しソースコードの修正を行う場合**

ブランチA所有者のUTにて共通部品にバグが発覚し修正が発生した場合

#### ブランチA所有者

1. 共通処理のバグ修正を行う
2. 共通処理の修正のみのコミットを作成する
3. コミットをブランチに反映する
4. ブランチB所有者にコミットを取り込むよう依頼する



#### ブランチB所有者

1. ノーマルシナリオ同様の手順を行い変更を取り入れる

